feat(gemini_eprintln): Add argument_mapper placeholder

This commit introduces a placeholder file `crates/gemini_utils/src/argument_mapper.rs`.

This file is intended to house the argument mapping logic for the `gemini_eprintln!` macro, laying the groundwork for future enhancements in how arguments are processed and mapped within the macro.