Fix: Add #[repr(C)] to FFI-bound structs in Rust

- Added `#[repr(C)]` to all structs in `tools/minizinc_ffi/src/types/mod.rs` that are passed across the FFI boundary.
- This addresses the `improper_ctypes` warnings and ensures correct memory layout when interacting with the C ABI.